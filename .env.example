# 齿轮箱选型系统 - 环境变量配置模板
# 复制此文件为 .env.local 并填入实际值

# ===========================================
# 认证配置 (PBKDF2-SHA256 哈希, 10万轮)
# ===========================================
# 生成方法 (使用项目内置脚本):
#   node scripts/generate-auth-hash.js <username> <password>
# 示例输出包含 REACT_APP_AUTH_SALT 和对应账号的 _HASH
#
# ⚠️ REACT_APP_AUTH_SALT 必填: 与用户名复合派生 salt, 避免跨账号哈希复用
# ⚠️ 三项任一缺失系统将拒绝登录, v2 起不再使用默认 fallback

REACT_APP_AUTH_SALT=请用 generate-auth-hash.js 生成 (32 字节 hex)
REACT_APP_ADMIN_HASH=请用 generate-auth-hash.js 生成 (admin 账号 PBKDF2 哈希)
REACT_APP_USER_HASH=请用 generate-auth-hash.js 生成 (user 账号 PBKDF2 哈希)

# ===========================================
# Sentry 错误监控 (可选, 见 src/config/sentry.js)
# ===========================================
# 不填则 Sentry 不启用
# REACT_APP_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
