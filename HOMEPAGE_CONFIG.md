# 配置说明 - Homepage 设置

## 问题说明

在将应用部署到子路径（如 `/gearbox`）时，需要在 `package.json` 中配置 `homepage` 字段，否则静态资源（CSS、JS文件）的路径会不正确。

## 两种部署场景

### 场景1：根路径部署
- 访问地址：`http://example.com/`
- 不需要配置 `homepage`
- 默认行为

### 场景2：子路径部署（推荐用于集成）
- 访问地址：`http://47.111.132.236/gearbox/`
- 需要配置 `homepage: "/gearbox"`
- 资源路径会自动添加前缀

## 解决方案

### 方法一：使用配置脚本（推荐）⭐

```bash
# 配置 homepage 为 /gearbox
./configure-homepage.sh /gearbox

# 重新构建
npm run build

# 部署
./deploy.sh
```

**优点**：
- 自动备份原配置
- 可随时恢复
- 灵活切换

### 方法二：手动修改 package.json

在 `package.json` 中添加：

```json
{
  "name": "gearbox-selection-system",
  "version": "1.0.0",
  "private": true,
  "homepage": "/gearbox",  // 添加这一行
  ...
}
```

### 方法三：环境变量（高级）

在构建时使用环境变量：

```bash
PUBLIC_URL=/gearbox npm run build
```

## 恢复原配置

如果使用了 `configure-homepage.sh`：

```bash
# 恢复原始 package.json
cp package.json.backup package.json

# 重新构建（用于本地开发或根路径部署）
npm run build
```

## 当前状态

当前 `package.json` **未配置** `homepage`，适用于：
- ✅ 本地开发（`npm start`）
- ✅ 根路径部署
- ❌ 子路径部署（需要先配置）

## 部署检查清单

**子路径部署前（http://47.111.132.236/gearbox/）**：
- [ ] 运行 `./configure-homepage.sh /gearbox`
- [ ] 运行 `npm run build` 重新构建
- [ ] 检查 `build/index.html` 中的资源路径是否包含 `/gearbox` 前缀
- [ ] 运行 `./deploy.sh` 部署

**根路径部署前（http://example.com/）**：
- [ ] 确保 `package.json` 中没有 `homepage` 或 `homepage: "/"`
- [ ] 运行 `npm run build`
- [ ] 部署

## 快速诊断

检查当前构建的资源路径：

```bash
# 查看 index.html 中的资源路径
grep -o 'src="[^"]*"' build/index.html
grep -o 'href="[^"]*"' build/index.html
```

**正确的子路径输出应该类似**：
```
src="/gearbox/static/js/main.xxx.js"
href="/gearbox/static/css/main.xxx.css"
```

**根路径输出**：
```
src="/static/js/main.xxx.js"
href="/static/css/main.xxx.css"
```

## 常见问题

### Q: 部署后页面空白或404？
**A**: 检查 `homepage` 配置是否与实际部署路径匹配

### Q: 样式丢失或资源加载失败？
**A**: 资源路径不正确，需要配置 `homepage`

### Q: 本地开发正常，部署后有问题？
**A**: 本地开发使用的是根路径，部署需要子路径配置

### Q: 如何同时支持多种部署方式？
**A**:
1. 保持默认配置用于本地开发
2. 部署前使用 `configure-homepage.sh` 配置
3. 或使用环境变量 `PUBLIC_URL`

## 建议

**推荐配置流程**：
1. 🔧 本地开发时不设置 `homepage`
2. 📦 部署到子路径时运行 `configure-homepage.sh`
3. 🚀 部署完成后可选择恢复原配置

这样既保证了开发便利性，又满足了部署需求。
