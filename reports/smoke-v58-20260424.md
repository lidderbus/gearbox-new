# v58 端到端烟测报告

**时间**: 2026-04-24
**版本**: APP_DATA_VERSION = 58
**部署地址**: http://47.99.181.195/gearbox-app/ (线上地址 qj-gearbox.duckdns.org/gearbox-app/)
**Bundle hash**: main.34a5142e.js (本地 ↔ 线上一致)

---

## 一、HTTP 可用性 — 全部通过

| 资源 | HTTP | 耗时 |
|------|------|------|
| index.html | 200 | 23 ms |
| asset-manifest.json | 200 | 45 ms |
| gearbox-data.json | 200 | 42 ms |
| manifest.json | 200 | 27 ms |
| favicon.ico | 200 | 28 ms |
| /fonts/NotoSansSC-Regular.ttf | 200 | 27 ms |

PDF 中文字体本地可加载 ✓,P1#4 的 sessionStorage 缓存策略有基础可依托。

---

## 二、回归测试 — 808/808 全绿

```
Test Suites: 19 passed, 19 total
Tests:       808 passed, 808 total
Time:        1.887 s
```

覆盖新增模块:
- `priceFormatter.test.js` (14 tests, v2 badge + 反查)
- `marketEnrichmentMerge.test.js` (5 tests)
- `pumpNPSHAdvisor.test.js` (5 tests)
- `iceClassThrust.test.js` (7 tests)
- `ptoThermalMargin.test.js` (6 tests)

---

## 三、数据层核查

| 指标 | 实测 | 备注 |
|------|------|------|
| gearboxPriceData 条目 | 246 | - |
| completeGearboxData 型号 | 585 | - |
| embeddedGearboxData 型号 | 628 (含 PTO/轴承变体) | - |
| complete 无价率 | 69.1% (404 条) | CLAUDE.md 记载"208"已过时,**建议更新** |
| TOP50 热销型号无价 | 19 条 | 已生成 `reports/top50-price-proposal.json` 待财务审核 |

---

## 四、UI 接入验证 (代码/bundle 双向确认)

### P0 新功能在 bundle 中的存在性 (Unicode 转义形式)

| 关键字 | Unicode escape | main.js | chunks |
|-------|---------------|---------|--------|
| 询价徽章 | `询价` | 1 | 15 |
| 船型预设 | `船型` | 1 | 13 |
| 修改参数 (Relaxation action) | `修改参数` | 1 | 0 |
| 一键生成完整文件包 | `一键生成完整文件包` | 1 | 0 |

### P2 新 advisor 在 bundle 中的存在性

| advisor | 关键字 | main.js | chunks |
|--------|--------|---------|--------|
| NPSH | NPSH (ASCII) | 1 | 0 |
| 冰级 | `冰级` | 1 | 1 |
| PTO 热功率 | `热功率` | 1 | 0 |

所有 UI 接入文本在线上 bundle 中均可查到,功能在可访问路径上。

### 关键数据贯通

- `onGenerateFullPackage`: App.js (L771, L1001) → SelectionResultTab (L33, L55) → EnhancedGearboxSelectionResult (L64, L348, L1249) ✓
- `iceClass` / `iceFactor`: useSelectionHandlers 计算 → result 注入 → EnhancedGearboxSelectionResult 顶部横幅 ✓
- 3 个 advisor 模块分别在 PumpInfoSection / EnhancedGearboxSelectionResult 内正确 import + 使用 ✓

---

## 五、构建信号

- `CI=false npm run build` 成功,仅剩两条第三方 sourcemap 警告 (html2pdf, mediapipe),与本次改动无关
- ESLint 无新增 error (CRA 默认 react-app 规则)
- Bundle 大小 ~41 MB (含 sourcemap),排除 .map 后约 11 MB

---

## 六、发现与建议

### 阻塞项 — 无

### 观察项 (P1/P2 范畴)
1. **CLAUDE.md 记录的"208 缺价"已过时** — 实际 404 条(69.1%)。建议更新文档以防误导后续协作者。
2. **TOP50 补录提案已生成但未合入**(`reports/top50-price-patch.txt`)。涉及财务审核折扣率,非技术可独立决定。
3. **第三方库 sourcemap 缺失** (html2pdf.js + mediapipe/tasks-vision),不影响生产,但开发态调试会略受影响。可选 `GENERATE_SOURCEMAP=false` 规避。
4. **Pareto/热力图 Tab 相对靠后**,新用户若不知"可视化对比"存在,可能错过。P0#5 审计时原标"orphaned"不准(已挂载),但**发现性**仍可改善(例如 Alert 提示"切换到「可视化对比」Tab 以查看三维比选")。
5. **NPSH 优雅降级**:当齿轮箱不需要备用泵(`needsPumpFlag=false`)或泵推荐失败时,NPSH advisor 不触发(预期行为,通过测试)。

### 待自然观察的试用期反馈
- 冰级下拉用户接受度
- 一键三联按钮使用频率
- 移动端离线横幅是否真的在弱网环境有价值

---

## 七、总结

v58 线上表现健康:
- ✅ 无 5xx / 404 / 超时
- ✅ 808 单测全绿
- ✅ 所有新功能在 bundle 中可达(Unicode 转义核实)
- ✅ 数据/算法/UI 三端一致

**推荐可进入试用期使用**。后续建议:
1. 24 小时线上观察 (错误率、PDF 生成成功率、首屏时间)
2. 采集前 10 个真实试用用户反馈 → 形成 P3 优先级
3. 财务审核 TOP50 补录清单,合入 v59
